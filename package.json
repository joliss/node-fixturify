{
  "name": "fixturify",
  "description": "Convert objects into directory structures and back again",
  "version": "0.3.4",
  "author": "Jo Liss <joliss42@gmail.com>",
  "main": "index.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/joliss/node-fixturify"
  },
  "dependencies": {
    "fs-extra": "^7.0.1",
    "matcher-collection": "^1.0.4"
  },
  "devDependencies": {
    "jshint": "~2.10.1",
    "rimraf": "^2.6.3",
    "tap": "^12.6.0"
  },
  "engines": {
    "node": "6.* || 8.* || >= 10.*"
  },
  "scripts": {
    "test": "jshint *.js && tap --timeout 2 ./test.js"
  }
}
